name: google_maps_native_sdk_example
description: Example app for google_maps_native_sdk
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=2.18.0 <4.0.0"
  flutter: ">=3.13.0"

dependencies:
  flutter: { sdk: flutter }
  google_maps_native_sdk:
    path: ..

flutter:
  uses-material-design: true

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^4.0.0
