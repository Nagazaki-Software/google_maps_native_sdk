name: google_maps_native_sdk
description: Native Google Maps plugin for Flutter/FlutterFlow with markers, polylines, caching, events — ideal for mobility apps (e.g., taxi).
version: 0.1.0
homepage: https://github.com/yourname/google_maps_native_sdk
repository: https://github.com/yourname/google_maps_native_sdk
issue_tracker: https://github.com/yourname/google_maps_native_sdk/issues

environment:
  sdk: ">=2.18.0 <4.0.0"
  flutter: ">=3.13.0"

flutter:
  plugin:
    platforms:
      android:
        package: com.example.google_maps_native_sdk
        pluginClass: GoogleMapsNativeSdkPlugin
      ios:
        pluginClass: GoogleMapsNativeSdkPlugin

dependencies:
  flutter: { sdk: flutter }
  plugin_platform_interface: ^2.1.8
  collection: ^1.18.0

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test: { sdk: flutter }
